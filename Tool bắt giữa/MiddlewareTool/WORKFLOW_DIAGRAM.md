# Workflow Diagram / Sơ Đồ Quy Trình

## Visual Workflow / Quy Trình Trực Quan

```
┌─────────────────────────────────────────────────────────────┐
│                     START GRADING SESSION                    │
│                    (Bắt đầu chấm bài)                        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   Tool Status: "Press F5 in client console to capture       │
│                 baseline for Stage 1"                        │
│   (Trạng thái: Bấm F5 để chụp baseline cho Stage 1)        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │  Client Console Shows:
                            │  "enter int "
                            │
                            ▼
                    ┌───────────────┐
                    │  Press F5     │ ◄─── USER ACTION
                    │  (Bấm F5)     │      (In Client Console)
                    └───────┬───────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   ✅ STAGE 1 BASELINE CAPTURED                              │
│   Tool Status: "Stage 1 baseline captured.                  │
│                 Waiting for user input (Press Enter)..."    │
│   (Đã chụp baseline Stage 1. Chờ nhập input...)            │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │  User Types in Console:
                            │  "enter int 1"
                            │
                            ▼
                    ┌───────────────┐
                    │  Press Enter  │ ◄─── USER ACTION
                    │  (Bấm Enter)  │      (In Client Console)
                    └───────┬───────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   🔍 COMPARING OUTPUTS                                       │
│   Baseline: "enter int "                                     │
│   Current:  "enter int 1"                                    │
│   Extracted Input: "1" ✓                                     │
│   (So sánh outputs, lấy input: "1")                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   ✅ STAGE 1 INPUT CAPTURED: "1"                            │
│   Tool Status: "Stage 1 input captured: '1'.                │
│                 Press F5 for next stage..."                  │
│   (Đã lưu input Stage 1. Bấm F5 cho stage tiếp...)         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │  Server Responds:
                            │  "banana"
                            │  "enter string "
                            │
                            ▼
                    ┌───────────────┐
                    │  Press F5     │ ◄─── USER ACTION
                    │  (Bấm F5)     │      (In Client Console)
                    └───────┬───────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   ✅ STAGE 2 BASELINE CAPTURED                              │
│   Tool Status: "Stage 2 baseline captured.                  │
│                 Waiting for user input..."                   │
│   (Đã chụp baseline Stage 2. Chờ nhập input...)            │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │  User Types:
                            │  "enter string hello"
                            │
                            ▼
                    ┌───────────────┐
                    │  Press Enter  │ ◄─── USER ACTION
                    │  (Bấm Enter)  │
                    └───────┬───────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   ✅ STAGE 2 INPUT CAPTURED: "hello"                        │
│   Tool Status: "Stage 2 input captured: 'hello'..."         │
│   (Đã lưu input Stage 2: "hello")                          │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            │  ... More Stages ...
                            │  (Repeat F5 → Enter)
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   STOP GRADING SESSION                       │
│                   (Dừng chấm bài)                           │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│   📊 SAVING RESULTS                                          │
│   - Excel file with 3 sheets                                 │
│   - Client/Server log files                                  │
│   - User inputs by stage                                     │
│   Tool Status: "Session stopped. 2 inputs captured          │
│                 across 2 stages."                            │
│   (Lưu kết quả. 2 inputs trong 2 stages)                   │
└─────────────────────────────────────────────────────────────┘
```

---

## State Transitions / Chuyển Trạng Thái

```
    [Session Not Running]
            │
            │ Click "Start Grading Session"
            ▼
    [Waiting for F5]
            │
            │ Press F5 in Client Console
            ▼
    [Stage N Baseline Captured]
            │
            │ User types input
            │ Press Enter in Client Console
            ▼
    [Stage N Input Captured]
            │
            ├─────────────────────┐
            │ Press F5           │ Click "Stop"
            ▼                     ▼
    [Next Stage Baseline] [Save Results & Exit]
```

---

## Data Flow / Luồng Dữ Liệu

```
┌──────────────┐
│ Client       │
│ Console      │ ─────┐
│ (User Input) │      │
└──────────────┘      │
                      │ F5: Capture Baseline
                      │ Enter: Capture Current
                      │
                      ▼
┌─────────────────────────────────────┐
│  ConsoleCaptureService              │
│  - CaptureConsoleOutput()           │
│  - Returns console text             │
└──────────────┬──────────────────────┘
               │
               │ Raw Console Output
               ▼
┌─────────────────────────────────────┐
│  MainWindow                         │
│  - OnCapturePressed() (F5)          │
│    → Save as baseline               │
│  - OnEnterPressed() (Enter)         │
│    → ExtractInputFromBaseline()     │
└──────────────┬──────────────────────┘
               │
               │ Extracted Input
               ▼
┌─────────────────────────────────────┐
│  Data Storage                       │
│  - _baselineCaptures (F5 data)      │
│  - _enterLines (Enter data)         │
│  - _stageCaptures (Full snapshots)  │
└──────────────┬──────────────────────┘
               │
               │ On Stop Session
               ▼
┌─────────────────────────────────────┐
│  ExcelLogger                        │
│  - AppendStagesToExcel()            │
│  - AppendClientStagesToExcel()      │
│  - AssignStagesToLogs()             │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Output Files                       │
│  - LogData.xlsx (3 sheets)          │
│  - Client.log                       │
│  - Server.log                       │
│  - EnterLines.log                   │
│  - UserInputs.log                   │
└─────────────────────────────────────┘
```

---

## Stage Management / Quản Lý Stage

```
Stage 1:
  ┌─────────────┐
  │ F5 Press    │ → Baseline Captured
  └─────┬───────┘   _currentStage = 1
        │           _baselineCaptures[1] = console output
        │
  ┌─────▼───────┐
  │ Enter Press │ → Input Extracted
  └─────────────┘   Compare with _baselineCaptures[1]
                    Extract: user input
                    Save to _enterLines[1]
                    Save to _stageCaptures[1]

Stage 2:
  ┌─────────────┐
  │ F5 Press    │ → Baseline Captured
  └─────┬───────┘   _currentStage = 2
        │           _baselineCaptures[2] = console output
        │
  ┌─────▼───────┐
  │ Enter Press │ → Input Extracted
  └─────────────┘   Compare with _baselineCaptures[2]
                    Extract: user input
                    Save to _enterLines[2]
                    Save to _stageCaptures[2]

... and so on ...
```

---

## Comparison Logic / Logic So Sánh

```
Baseline Capture (F5):
┌──────────────────────┐
│ Console Output:      │
│ "Welcome"            │
│ "enter int "         │ ◄─── Last non-empty line
│ ""                   │
└──────────────────────┘

Current Output (Enter):
┌──────────────────────┐
│ "Welcome"            │
│ "enter int 1"        │ ◄─── Matches + has extra content
│ ""                   │
└──────────────────────┘

Extraction:
  Baseline: "enter int "
  Current:  "enter int 1"
  Result:   "1" ✓
```

---

## Error Handling / Xử Lý Lỗi

```
Scenario 1: Enter without F5
┌─────────────┐
│ Enter Press │ → No baseline exists
└─────┬───────┘   _currentStage = 0
      │           _baselineCaptures is empty
      ▼
[Ignore Enter Press]
[Status: No change]


Scenario 2: F5 multiple times
┌─────────────┐
│ F5 Press    │ → Stage 1 baseline
└─────┬───────┘
      │
┌─────▼───────┐
│ F5 Press    │ → Stage 2 baseline (overwrites)
└─────┬───────┘   _currentStage = 2
      │
┌─────▼───────┐
│ Enter Press │ → Uses Stage 2 baseline
└─────────────┘


Scenario 3: Input extraction fails
┌─────────────┐
│ Enter Press │ → Can't match baseline
└─────┬───────┘
      ▼
[Return empty string]
[Status: Orange warning]
[Suggest: Press F5 again]
```

---

## Summary / Tóm Tắt

**Key Points:**
1. **F5** = Capture baseline = Create new stage
2. **Enter** = Extract input = Complete current stage
3. Each stage needs: F5 first, then Enter
4. Status bar shows current state
5. Excel saves all data on stop

**Điểm Chính:**
1. **F5** = Chụp baseline = Tạo stage mới
2. **Enter** = Lấy input = Hoàn thành stage hiện tại
3. Mỗi stage cần: F5 trước, Enter sau
4. Status bar hiện trạng thái
5. Excel lưu tất cả khi dừng
